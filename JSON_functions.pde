JSONObject load_JSON(String URL) {
  return loadJSONObject(URL);
}

JSONArray load_JSONArray(String URL) {
  return loadJSONArray(URL);
}

void save_JSON(JSONObject a, String URL) {
  saveJSONObject(a, URL);
}